#条件判断
if x:
	pass
else:
	print(x)
	
# s = input() 输入50 输出为字符串'50' 

#循环
# for 和 while
#所以for x in ...循环就是把每个元素代入变量x，然后执行缩进块的语句。
#第二种循环是while循环，只要条件满足，就不断循环，条件不满足时退出循环。
#break 提前退出循环 continue 跳过本次循环 开始下一次循环